# Runtime (matches pyproject.toml). Install: pip install -r requirements.txt
python-dotenv>=1.0.0
pydantic>=2.0
smolagents
duckduckgo-search>=6.0
streamlit>=1.28

# Dev (optional): pip install pytest>=8.0
